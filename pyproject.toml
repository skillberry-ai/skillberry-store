[project]
name = "skillberry-store"
version = "0.1.0"
description = "SkillBerry Store service"
authors = [{ name = "Eran Raichstein", email = "eranra@il.ibm.com" }, 
            { name = "David Breitgand", email = "davidbr@il.ibm.com" }, 
            { name = "Avi Weit", email = "weit@il.ibm.com" }, 
            { name = "Erez Hadad", email = "erezh@il.ibm.com" },
            { name = "Osher Elhadad", email = "Osher.Elhadad@ibm.com" },
            { name = "Priel Hazan", email = "priel.hazan@ibm.com" }]
requires-python = ">=3.11"
dependencies = [
  "annotated-types>=0.7.0",
  "anyio>=4.5.0",
  "asgiref>=3.8.1",
  "attrs>=25.1.0",
  "bcrypt>=4.0.0",
  "certifi",
  "charset-normalizer",
  "click>=8.1.8",
  "configobj>=5.0.9",
  "distro>=1.9.0",
  "dnspython>=2.7.0",
  "docker>=7.1.0",
  "docstring-parser>=0.16",
  "email-validator>=2.2.0",
  "fastapi>=0.115.12",
  "fastapi-mcp>=0.3.7",
  "fastapi-versioning>=0.10.0",
  "fasteners>=0.19",
  # Bounded on both sides deliberately (PR #308 review issue #11). The model
  # string "sentence-transformers/all-MiniLM-L6-v2" is pinned in
  # vdbs/vector_db_interface.py, and the truncation-limit pin added for issue #10
  # reaches through the internal `.model.tokenizer` path — neither of which is a
  # public API contract. fastembed is pre-1.0, so its minor releases are the
  # breaking increments: an unbounded dependency turns "renamed or dropped the
  # model" into a failure at first embed rather than at build. Raise the upper
  # bound deliberately, after checking the model string still resolves and the
  # encoder tests pass.
  "fastembed>=0.8.0,<0.9",
  "filelock>=3.17.0",
  "fsspec>=2025.2.0",
  "h11>=0.13.0",
  "httpcore>=1.0.7",
  "httpx>=0.28.1",
  "httpx-sse>=0.4.0",
  "idna",
  "jinja2>=3.1.5",
  "jiter>=0.9.0",
  "jsonpatch>=1.33",
  "jsonpointer>=3.0.0",
  "langchain-core>=0.3.49",
  "langchain-mcp-adapters>=0.0.6",
  "langchain-openai>=0.3.1",
  "langgraph>=0.2.68",
  "langgraph-checkpoint>=2.1.0",
  "langgraph-prebuilt>=0.5.2",
  "langgraph-sdk>=0.1.70",
  "langsmith>=0.3.22",
  "markdown-it-py>=3.0.0",
  "markupsafe>=3.0.2",
  "mcp>=1.4.1",
  "mdurl>=0.1.2",
  "numpy>=1.26.4",
  "orjson>=3.10.16",
  "packaging>=24.2",
  "pathspec>=0.12.1",
  "pillow>=11.1.0",
  "platformdirs>=4.3.6",
  "pydantic>=2.11.1",
  "pydantic-core",
  "pydantic-settings>=2.8.1",
  "pygments>=2.19.1",
  "python-dateutil>=2.9.0.post0",
  "python-dotenv>=1.0.1",
  "python-multipart>=0.0.6",
  "pyyaml>=6.0.2",
  "regex>=2024.11.6",
  "requests",
  "requests-toolbelt>=1.0.0",
  "rfc3986>=1.5.0",
  "rich>=13.9.4",
  "ruamel-yaml>=0.18.10",
  "ruamel-yaml-clib>=0.2.12",
  "six>=1.17.0",
  "sniffio>=1.3.1",
  "sse-starlette>=2.1.3,<3.0.0",
  "starlette>=0.45.3,<1.0.0",
  "tenacity>=9.0.0",
  "tiktoken>=0.9.0",
  "toml>=0.10.2",
  "tqdm>=4.67.1",
  "typer>=0.15.2",
  "typing-extensions>=4.12.2",
  "urllib3>=1.25.3",
  "uvicorn>=0.23.1",
  "xxhash>=3.5.0",
  "zstandard>=0.23.0",
  "prometheus_client>=0.21.1",
  "opentelemetry-api",
  "opentelemetry-sdk",
  "opentelemetry-exporter-otlp",
  "opentelemetry-instrumentation-fastapi",
  "opentelemetry-instrumentation-requests",
  "faiss-cpu",
  "chromadb",
  "lancedb",
  "pandas",
  "wsgidav>=4.3",
  "cheroot>=10.0",
  "shenanigaNFS @ git+https://github.com/JordanMilne/ShenanigaNFS.git",
  # NOTE: bundled plugins are deliberately NOT core dependencies -- they live in
  # the "plugin-*" extras below (and "plugins-all"). Listing any of them here
  # would make it impossible to build a core-only image, since entry-point
  # auto-loading imports every installed plugin at startup.
]

[project.optional-dependencies]

# Development dependencies: linting, type-checking, formatting.
# Install with: make install-requirements ODEPS=dev
dev = [
  "black>=22.3.0",
  "flake8>=3.9.0",
  "mccabe>=0.6.1",
  "mypy>=0.910",
  "mypy-extensions>=0.4.4",
  "pycodestyle>=2.7.0",
  "pyflakes>=2.3.1",
  "ruff>=0.9.9",
  "skillberry-store-sdk",
]

# Build/codegen tools: Rust build tools, SDK generation (includes jdk4py ~180 MB).
# Install with: make install-requirements ODEPS=build
build = [
  "cargo",
  "maturin>=1.8.4",
  "openapi-generator-cli[jdk4py]==7.20.0",
  "openapi-generator-cli",
  "openapi-python-client>=0.24.0",
  "setuptools>=75.8.2",
  "toml-cli",
  "tomli",
  "tomli-w",
  "wheel",
]

# Test dependencies: test runner and plugins.
# The `test` extra at the bottom includes this via dev + plugins-all.
pytest = [
  "iniconfig>=2.0.0",
  "pluggy>=1.5.0",
  "pytest>=8.3.4",
  "pytest-asyncio>=0.25.2",
]

# Plugin dependencies
plugin-creator = [
  "skillberry-plugin-creator>=0.2.0",
]

plugin-evaluator = [
  "skillberry-plugin-evaluator>=0.2.0",
]

plugin-security = [
  "skillberry-plugin-security>=0.1.0",
]

plugin-dedupe = [
  "skillberry-plugin-dedupe>=0.1.0",
]

plugin-mcp-importer = [
  "skillberry-plugin-mcp-importer>=0.1.0",
]

plugin-anthropic-skill-generator = [
  "skillberry-plugin-anthropic-skill-generator>=0.1.0",
]

plugin-kagenti-approver = [
  "skillberry-plugin-kagenti-approver>=0.1.0",
]

plugin-sast = [
  "skillberry-plugin-sast>=0.1.0",
  "llm-switchboard"
]

plugin-skill-optimizer = [
  "skillberry-plugin-skill-optimizer>=0.1.0",
]

plugin-simulate = [
  "skillberry-plugin-simulate>=0.1.0",
]

plugin-provenance = [
  "skillberry-plugin-provenance>=0.1.0",
]

plugin-doc-generator = [
  "skillberry-plugin-doc-generator>=0.1.0",
]

plugin-dependency-tracker = [
  "skillberry-plugin-dependency-tracker>=0.1.0",
]

plugin-ask-runspace = [
  "skillberry-plugin-ask-runspace>=0.1.0",
]

plugin-dast = [
  "skillberry-plugin-dast>=0.1.0",
]

plugin-skillssh-importer = [
  "skillberry-plugin-skillssh-importer>=0.1.0",
]

# Install all plugins
plugins-all = [
  "skillberry-plugin-creator>=0.2.0",
  "skillberry-plugin-evaluator>=0.2.0",
  "skillberry-plugin-security>=0.1.0",
  "skillberry-plugin-dedupe>=0.1.0",
  "skillberry-plugin-mcp-importer>=0.1.0",
  "skillberry-plugin-anthropic-skill-generator>=0.1.0",
  "skillberry-plugin-kagenti-approver>=0.1.0",
  "skillberry-plugin-sast>=0.1.0",
  "skillberry-plugin-skill-optimizer>=0.1.0",
  "skillberry-plugin-simulate>=0.1.0",
  "skillberry-plugin-provenance>=0.1.0",
  "skillberry-plugin-doc-generator>=0.1.0",
  "skillberry-plugin-dependency-tracker>=0.1.0",
  "skillberry-plugin-ask-runspace>=0.1.0",
  "skillberry-plugin-dast>=0.1.0",
  "skillberry-plugin-skillssh-importer>=0.1.0",
]

test = [
  "skillberry-store[dev,pytest,plugins-all]"
]

[tool.uv.sources]
skillberry-store-sdk = { path = "client/python/skillberry_store_sdk", editable = true }
skillberry-plugin-creator = { path = "plugins/skillberry-plugin-creator", editable = true }
skillberry-plugin-evaluator = { path = "plugins/skillberry-plugin-evaluator", editable = true }
skillberry-plugin-security = { path = "plugins/skillberry-plugin-security", editable = true }
skillberry-plugin-dedupe = { path = "plugins/skillberry-plugin-dedupe", editable = true }
skillberry-plugin-mcp-importer = { path = "plugins/skillberry-plugin-mcp-importer", editable = true }
skillberry-plugin-anthropic-skill-generator = { path = "plugins/skillberry-plugin-anthropic-skill-generator", editable = true }
skillberry-plugin-kagenti-approver = { path = "plugins/skillberry-plugin-kagenti-approver", editable = true }
skillberry-plugin-sast = { path = "plugins/skillberry-plugin-sast", editable = true }
skillberry-plugin-skill-optimizer = { path = "plugins/skillberry-plugin-skill-optimizer", editable = true }
skillberry-plugin-simulate = { path = "plugins/skillberry-plugin-simulate", editable = true }
skillberry-plugin-provenance = { path = "plugins/skillberry-plugin-provenance", editable = true }
skillberry-plugin-doc-generator = { path = "plugins/skillberry-plugin-doc-generator", editable = true }
skillberry-plugin-dependency-tracker = { path = "plugins/skillberry-plugin-dependency-tracker", editable = true }
skillberry-plugin-ask-runspace = { path = "plugins/skillberry-plugin-ask-runspace", editable = true }
skillberry-plugin-dast = { path = "plugins/skillberry-plugin-dast", editable = true }
skillberry-plugin-skillssh-importer = { path = "plugins/skillberry-plugin-skillssh-importer", editable = true }
llm-client = { path = "skillberry-common/packages/llm-client", editable = true }

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools]
include-package-data = true

[tool.setuptools.package-data]
skillberry_store = ["ui/**/*"]

[project.scripts]
sbs-srv = "skillberry_store.main:main"
