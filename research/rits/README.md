# Simple usage of the IBM rits inference service

## How to run
1. Define the `RITS_API_KEY` environment variable
```bash
export RITS_API_KEY=********************************
```
2. Execute the code

```bash
python main.py
```