name: Skill Simulator
slug: simulate
version: 0.1.0
description: Run skills in isolated Docker sandboxes
plugin_type: evaluator
sdk_version: ^0.1
has_api: true
required_env:
  - name: DOCKER_HOST
    description: Docker daemon socket (uses default if unset)
    required: false
